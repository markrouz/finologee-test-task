How to run:

./mvnw clean install && docker-compose up

To test the API you can use provided postman collection: demo.postman_collection.json

All main requirements are done

Optional requirements are done except 2 and 3 (paging and fraud)